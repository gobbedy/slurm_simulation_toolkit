\documentclass[]{article}
\usepackage{pgfplots}

\begin{document}

	\pgfmathdeclarefunction{gauss}{2}{%
		\pgfmathparse{1/(#2*sqrt(2*pi))*exp(-((x-#1)^2)/(2*#2^2))}%
	}

	\begin{tikzpicture}
		\begin{axis}[]
			%\addplot [domain=MIN_BUCKET_EDGE:MAX_BUCKET_EDGE, samples=401]{ gauss(TEST_ERROR_MEAN,TEST_ERROR_STDV) };
			%\addplot [domain=MIN_BUCKET_EDGE:MAX_BUCKET_EDGE, samples=401]{ gauss(4.21884,0.137043) };
			%\addplot [hist={density,bins=35,data min=MIN_BUCKET_EDGE,data max=MAX_BUCKET_EDGE}] table [y index=0] {
            %\addplot [domain=3.8:4.7, samples=401]{ gauss(4.21884,0.137043) };
			%\addplot [hist={density,bins=35,data min=3.8,data max=4.7}] table [y index=0] {
			\addplot [domain=MIN_BUCKET_EDGE:MAX_BUCKET_EDGE, samples=401]{ gauss(TEST_ERROR_MEAN,TEST_ERROR_STDV) };
			\addplot [hist={density,bins=35,data min=MIN_BUCKET_EDGE,data max=MAX_BUCKET_EDGE}] table [y index=0] {
			TEST_ERROR_LIST
		};
		\end{axis}
	\end{tikzpicture}
\end{document}